import { Typography } from '@mui/material'
import React from 'react'

function CustomSubHeading({ value }) {
  return (
    <Typography variant="body1" >{value}</Typography>
  )
}

export default CustomSubHeading